# Це файл robots.txt, який дає інструкції пошуковим системам, як індексувати ваш сайт.
# В даному випадку ми дозволяємо доступ до всіх сторінок.

User-agent: *
Allow: /

# Тут вказано шлях до файлу sitemap, де пошукові системи можуть знайти перелік усіх сторінок.
# Якщо ви працюєте локально, використовуємо "localhost". У фінальній версії на хостингу потрібно вказати реальний домен.

Sitemap: http://localhost:5500/sitemap.xml  # Поки що використовуємо локальний сервер, після розгортання на хостингу замініть на ваш реальний домен
